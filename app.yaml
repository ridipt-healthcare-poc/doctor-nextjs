runtime: nodejs20
service: doctor

instance_class: F2

automatic_scaling:
  min_instances: 1
  max_instances: 3

env_variables:
  NODE_ENV: production
  NEXT_PUBLIC_API_URL: "https://backend.medsparsh.com"
